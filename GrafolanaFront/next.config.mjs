/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // Warning: Only disable this for temporary testing
      ignoreDuringBuilds: true,
    },
  };

export default nextConfig;
